/// Wait time before opening another dialog to make transition animations
/// look better.
const int WAIT_BETWEEN_DIALOGS_MILLISECONDS = 50;

const Duration ANIMATED_SWITCHER_DEFAULT_DURATION = Duration(milliseconds: 100);
